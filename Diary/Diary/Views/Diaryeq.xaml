<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Diary.ViewModels"
             x:Class="Diary.Views.Diaryeq"
             BackgroundColor="Gray">
    <ContentPage.BindingContext>
        <viewmodels:DiaryqeViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="*,Auto,Auto,Auto,*" VerticalOptions="CenterAndExpand">
        <Entry Grid.Row="0" Text="{Binding CountString}" />
        <Label
                x:Name="labelCount"
                Grid.Row="1"
                   FontSize="22"
                Text="{Binding CountString}"
                   BackgroundColor="GreenYellow"
                VerticalOptions="CenterAndExpand"
                HorizontalOptions="CenterAndExpand" />
        <Button  x:Name="counter" Command="{Binding IncreaseCount}" Grid.Row="2" Text="click" HorizontalOptions="Center" />
    </Grid>
</ContentPage>